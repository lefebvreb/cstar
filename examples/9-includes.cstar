/* You can use include directives at the beginning of any file, to include code from other modules. */
include "examples/std.cstar";

System main() {
    /* Uses the max function, defined in an other file. */
    println("max(42, 13) = ", max(42, 13));
}

Init [main];
Run [];